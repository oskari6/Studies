objects.forEach(function(objectA, i){
    for(var j = i + 1; j < objects.length; j++){
        //evaluate reference using h , ie:
        var objectB = objects[j];
        //perform collision detection between objectA and objectB
    }
})